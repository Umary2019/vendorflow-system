# Local development (Express server running on port 5000)
VITE_API_URL=http://localhost:5000

# Vercel monorepo deployment (frontend + API same domain)
# VITE_API_URL=/api

# Separate backend deployment (Render/Railway/etc)
# VITE_API_URL=https://your-backend-url
